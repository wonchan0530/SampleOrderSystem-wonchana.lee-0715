# 생산라인

> 상위 문서: [`../PRD.md`](../PRD.md) §5.7, §2

## 개요

주문 승인 시 재고가 부족하면 자동으로 생산라인 큐에 등록되어 부족분을 생산한다. 하나의 생산라인은 한 번에 시료 하나를 생산하며, 대기 중인 생산 작업은 **FIFO(선입선출)** 로 처리된다.

## 핵심 계산식

| 항목 | 계산식 |
|---|---|
| 부족분 | `주문 수량 - 현재 재고 수량` (주문 승인 시점 기준) |
| 실생산량 | `ceil(부족분 / 수율)` — 수율로 인한 불량품 발생을 감안해 필요한 만큼보다 더 많이 생산 |
| 총생산시간 | `평균 생산시간 * 실생산량` |

예시: 부족분 5, 수율 0.9 → 실생산량 = `ceil(5 / 0.9)` = `ceil(5.56)` = **6개**. 평균 생산시간이 2(단위)라면 총생산시간 = `2 * 6` = **12**.

## 생산 큐 (대기열)

- 주문 승인 시 재고 부족으로 `PRODUCING` 전환된 주문은 생산 큐에 순서대로 등록된다.
- **스케줄링 전략: FIFO** — 먼저 등록된 생산 작업이 먼저 처리된다.
- 한 번에 하나의 생산 작업만 실제로 "생산 중" 상태가 되며, 나머지는 대기 상태로 큐에 남는다.

## 하위 기능

### 1) 생산 현황 표기
- 현재 생산 중인 시료에 대한 정보를 표시한다.
- 표시 수준은 자유롭게 결정 가능하나 최소한 다음을 포함하는 것을 권장한다.
  - 생산 중인 시료명/주문 정보
  - 실생산량, 총생산시간
  - 현재까지의 생산 진행량(또는 남은 시간)

### 2) 대기 주문 확인
- 생산 큐에서 대기 중인 생산 작업 목록을 FIFO 순서대로 출력한다.
- 표시 항목(예): 대기 순번, 주문 ID, 시료명, 실생산량

### 3) 생산 완료 처리
- 생산이 완료되면(총생산시간 경과 또는 콘솔 명령으로 완료 처리) 다음을 수행한다.
  1. 생산된 실생산량 중 정상품 수량만큼 재고에 반영한다. (정상품 수량은 수율 적용 결과이며, 최소한 부족분만큼은 충족되어야 함)
  2. 해당 주문 상태를 `PRODUCING` → `CONFIRMED`로 전환한다.
  3. 큐에서 다음 대기 작업이 있으면 이어서 생산을 시작한다(FIFO).

> 콘솔 기반 시스템 특성상 "총생산시간 경과"를 실제 시간 흐름으로 시뮬레이션할지, 담당자가 콘솔에서 "생산 완료" 명령을 입력해 수동으로 진행시킬지는 구현 시 결정한다. 명세(PDF)에는 생산 완료 트리거의 상세 방식이 명시되어 있지 않다.

## 예외/유효성 처리

| 상황 | 처리 |
|---|---|
| 생산 큐가 비어있는 상태에서 대기 주문 확인 | "대기 중인 생산 작업 없음" 표시 |
| 생산 중인 작업이 없는 상태에서 생산 현황 조회 | "현재 생산 중인 시료 없음" 표시 |

## 관련 문서

- 재고 부족 판정 및 `PRODUCING` 전환: [`03-주문관리.md`](03-주문관리.md)
- 생산 완료 후 출고: [`05-출고처리.md`](05-출고처리.md)
- 재고/생산 현황 집계: [`04-모니터링.md`](04-모니터링.md)
