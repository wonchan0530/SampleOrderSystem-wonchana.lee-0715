# 출고처리

> 상위 문서: [`../PRD.md`](../PRD.md) §5.6

## 개요

재고가 충분해져 `CONFIRMED` 상태가 된 주문에 대해 실제 출고를 실행하는 기능. 출고가 완료되면 주문은 시스템상 최종 상태인 `RELEASE`로 전환된다.

## 하위 기능

### 1) 출고 대상 주문 확인
- `CONFIRMED` 상태의 주문 목록을 표시한다.
- 표시 항목(예): 주문 ID, 시료명, 고객명, 주문 수량

### 2) 출고 실행
- **입력값**: 출고할 주문 ID
- **처리**:
  - 대상 주문이 `CONFIRMED` 상태인지 확인한다. 아니라면 거부한다.
  - 주문 상태를 `RELEASE`로 전환한다.
- **재고 반영**: 재고 차감은 승인 시점(§ [`03-주문관리.md`](03-주문관리.md) 주문 승인 로직)에 이미 이루어졌으므로, 출고 처리 시점에 재고를 추가로 차감하지 않는다.

> **확정**: 재고 차감 시점은 (a) 주문 승인 시 즉시 차감으로 확정해 구현했다(`OrderController::approve`). 출고(`OrderController::release`)는 상태를 `CONFIRMED` → `RELEASE`로 전환하기만 하는 순수 전이이며, 재고에는 관여하지 않는다.

## 예외/유효성 처리

| 상황 | 처리 |
|---|---|
| `CONFIRMED`가 아닌 주문에 출고 시도 | 거부, 에러 메시지 |
| 존재하지 않는 주문 ID 지정 | 거부, 에러 메시지 |
| 이미 `RELEASE`된 주문에 재출고 시도 | 거부, 에러 메시지 |

## 관련 문서

- 주문 상태 전이 전체: [`03-주문관리.md`](03-주문관리.md)
- 출고 완료 후 집계: [`04-모니터링.md`](04-모니터링.md)
